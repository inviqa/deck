{
  "name": "{{ SUBTHEME }}",
  "version": "1.0.0",
  "description": "Libraries required for {{ SUBTHEME }}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
